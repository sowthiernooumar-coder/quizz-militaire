[phases.setup]
aptPkgs = ["npm"]

[phases.install]
cmds = [
    "composer install --no-dev --optimize-autoloader --no-interaction --ignore-platform-reqs",
    "npm ci",
    "npm run build"
]

[start]
cmd = "bash start.sh"
